apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}   # Simplest issuer for testing (Replace selfSigned with Let's Encrypt (ACME issuer) for real public certificates.)