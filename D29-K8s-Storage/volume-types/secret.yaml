apiVersion: v1
kind: Pod
metadata:
  name: secret-example
spec:
  containers:
  - name: app
    image: busybox
    volumeMounts:
    - mountPath: /etc/secret
      name: secret-vol
  volumes:
  - name: secret-vol
    secret:
      secretName: db-secret