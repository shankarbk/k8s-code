apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-data
spec:
  storageClassName: fast-ssd                # ← reference the StorageClass name
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi