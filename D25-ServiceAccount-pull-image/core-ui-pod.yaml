apiVersion: v1
kind: Pod
metadata:
  name: core-ui-pod
  labels:
    app: core-ui
spec:
  serviceAccountName: docker-sa
  containers:
  - name: core-ui-app
    image: sbkqubits/core-ui:1.0
    ports:
    - containerPort: 80
